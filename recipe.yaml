package:
  name: uuid
  version: "0.1.0"

source:
  path: .

build:
  script:
    - mkdir -p $PREFIX/lib/mojo
    - cp -r uuid $PREFIX/lib/mojo/

requirements:
  host:
    - mojo ==0.26.3.0.dev2026042005
  run:
    - mojo ==0.26.3.0.dev2026042005

about:
  summary: Fast UUID v4 and v7 generation for Mojo with SIMD-accelerated hex encoding
  license: MIT
